# Web 框架
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# HTTP 客户端
httpx>=0.27,<1.0

# OpenAI 官方 SDK（v1）
openai>=1.40.0,<2.0

# 企业微信 SDK（带企业加密能力）
xmltodict==0.13.0
wechatpy>=1.8.18,<2.0

# AES-CBC 解密
cryptography>=42,<45


# 会话记忆（可选：若 CHAT_MEMORY=redis）
redis>=5.0.1,<6.0.0


# 上传解析等（可选，FastAPI 处理表单/文件时需要）
python-multipart>=0.0.9,<0.0.10

# 性能更好的 JSON 序列化（可选，FastAPI 会自动使用）
orjson>=3.10,<4.0

pymupdf==1.24.9
pypdf>=4.2
Pillow>=10.3
# 本地 OCR（可选，若启用 LOCAL_OCR_ENABLE=true 才需要，且系统要装 tesseract）
pytesseract>=0.3.10

# FastAPI 现已基于 Pydantic v2，手动钉一下兼容范围更稳
pydantic>=2.5,<3.0
pydantic-settings>=2.2,<3.0
